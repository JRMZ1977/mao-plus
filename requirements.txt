# MAO Plus — Dependencias Python
# Fase 2: Backend FastAPI para análisis morfométrico

# ── Servidor web ─────────────────────────────────────────────────────────────
fastapi==0.128.8
uvicorn[standard]==0.39.0
python-multipart==0.0.20

# ── Análisis de imagen ────────────────────────────────────────────────────────
opencv-python-headless==4.13.0.92   # cv2: contornos, morfología, bordes, color
                                    # ÚNICO proveedor de cv2. El pin defensivo de
                                    # opencv-python se retiró junto con ultralytics
                                    # (era quien lo exigía sin pin y colaba OpenCV 5.x).
numpy==2.0.2                        # operaciones matriciales
pillow==11.3.0                      # lectura/escritura de imágenes (PIL)
scikit-image==0.24.0                # GLCM textura, LBP, watershed
imageio==2.37.2                     # soporte de formatos adicionales

# ── Geometría y métricas ──────────────────────────────────────────────────────
shapely==2.0.7                      # Polygon: área, perímetro, validación
trimesh==4.5.3                      # mallas 3D: lectura OBJ + muestreo de superficie

# ── Estadística y ML ─────────────────────────────────────────────────────────
scipy==1.13.1                       # ConvexHull, ndimage, stats, signal
scikit-learn==1.6.1                 # PCA, StandardScaler, KMeans, silhouette — comparator.py (CMO)

# ── Inferencia IA (MobileSAM ONNX) ───────────────────────────────────────────
onnxruntime==1.19.2             # runtime ONNX CPU — MobileSAM encoder+decoder
#
# ultralytics: RETIRADO (2026-07-19). No lo importa ningún módulo — la rama YOLO
# se borró en bba1910 (2026-06-12) y el runtime de SAM es onnxruntime, no torch.
# Su único uso era exportar MobileSAM .pt → .onnx, un proceso OFFLINE y de una
# sola vez cuyo procedimiento vive en python/modules/sam_segmenter.py
# (download_models genera python/models/README.md con los pasos).
# NO lo reintroduzcas aquí ni en requirements-dev.txt: arrastra torch +
# torchvision + matplotlib + polars + nvidia-ml-py (≈514 MB) y exige
# opencv-python sin pin, que eclipsa al headless con OpenCV 5.x.
# Para exportar, instálalo en un venv desechable (ver el README generado).

# ── Utilidades ────────────────────────────────────────────────────────────────
python-dotenv==1.2.1
pyyaml==6.0.3
piexif==1.1.3                       # EXIF: extracción de metadatos — scale.py:130

# ── NO instalado aún (Fase 3+) ────────────────────────────────────────────────
# pandas              # exportación CSV estructurada
# matplotlib          # gráficas estáticas (radar, scatter). Ya NO se cuela por la
#                       puerta de atrás: entraba como transitiva de ultralytics.
# reportlab           # generación de PDF en Python
# rawpy               # lectura de archivos RAW fotográficos
# exifread            # extracción de metadatos EXIF
